{
  "mcpServers": {
    "healthcare-mcp": {
      "command": "npx",
      "args": ["-y", "healthcare-mcp-public"],
      "description": "Healthcare: PubMed, FDA drugs, ClinicalTrials, CID-10, medRxiv, NCBI Bookshelf, DICOM, calculadoras medicas",
      "priority": "critical",
      "notes": "Essencial para medicos. Acesso a PubMed, trials clinicos, CID-10, FDA."
    },
    "pubmed-mcp": {
      "command": "npx",
      "args": ["-y", "@cyanheads/pubmed-mcp-server"],
      "description": "PubMed avancado: busca, abstracts, artigos relacionados, analise de literatura biomedica",
      "priority": "critical",
      "notes": "39M+ citacoes. Busca avancada com filtros por data, tipo de estudo, MeSH terms."
    },
    "biomcp": {
      "command": "uvx",
      "args": ["biomcp", "run"],
      "description": "BioMCP: PubMed, ClinicalTrials.gov, MyVariant.info. Queries biomedicas em linguagem natural.",
      "priority": "high",
      "notes": "GenomOncology. Busca de variantes geneticas, trials por condicao, literature review automatica."
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Documentacao atualizada em tempo real para qualquer lib/framework. Adicione 'use context7' nos prompts.",
      "priority": "critical",
      "notes": "Free tier: 1000 req/mo, 60 req/h. Essencial para codigo atualizado."
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/organizacao"],
      "description": "Acesso a arquivos do projeto",
      "priority": "critical"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub: issues, PRs, repos, code search",
      "priority": "high"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "description": "Busca e download de conteudo web",
      "priority": "high"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistencia de contexto entre sessoes",
      "priority": "high"
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Busca web via Brave (2000 req/mo free)",
      "priority": "medium"
    },
    "sqlite": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite", "data/knowledge.db"],
      "description": "Base de dados local para knowledge base",
      "priority": "medium"
    },
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "description": "Automacao de browser para scraping",
      "priority": "low"
    }
  }
}
